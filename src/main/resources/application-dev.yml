server:
  port: 8080
  
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      order_inserts: true
      dialect: org.hibernate.dialect.PostgreSQLDialect
      
  datasource:
    url: jdbc:mysql://localhost:3306/colegio
    username: root
    password: 0423